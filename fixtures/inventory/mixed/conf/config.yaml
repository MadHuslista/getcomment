# @package bridge

defaults:
  - logging: default
  - _self_

streams:
  reference:
    nominal_srate_hz: 500

mode: active_send  # modbus_rtu | active_send

logging:
  file: ${hydra:run.dir}/run.log
